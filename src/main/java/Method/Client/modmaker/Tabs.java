package Method.Client.modmaker;

public enum Tabs {
    Render,
    Events,
    In_Pkt,
    Out_Pkt,
    Variables,
    VarHelper,
    Sub, // not a real tab, but used for sub blocks
    Search, // not a real tab, but used for Searching
    Math,
    Logic,
    Arrays,
    NewItem,
    Settings,
    Utils,
    Game,
    Entity,



}
